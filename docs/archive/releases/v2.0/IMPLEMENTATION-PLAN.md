# V2.0 P0 实施计划 - 任务提醒与团队协作

**创建日期**: 2026-01-26
**版本**: V2.0 P0
**状态**: 实施中 🚧

---

## 📋 目录

1. [功能A: 任务提醒和推送](#功能a-任务提醒和推送)
2. [功能B: 团队协作看板](#功能b-团队协作看板)
3. [技术架构](#技术架构)
4. [实施顺序](#实施顺序)
5. [验证方案](#验证方案)

---

## 功能A: 任务提醒和推送

### 1.1 功能概述

**目标用户**: 一线运营人员、一线管理者

**核心价值**:
- 任务到期自动提醒，避免遗漏
- 任务分配即时通知，提升响应速度
- 重要事件推送，确保关键信息触达

### 1.2 功能设计

#### 1.2.1 通知类型

**A. 任务到期提醒**
- 触发时机：任务截止日期前3天、1天、当天
- 通知内容：商户名称、任务标题、剩余天数
- 优先级：根据任务priority字段（urgent > high > medium > low）

**B. 任务分配通知**
- 触发时机：新任务创建并分配给用户时
- 通知内容：商户名称、任务类型、分配人
- 优先级：高

**C. 任务状态变更通知**
- 触发时机：任务阶段切换（planning → executing → evaluating）
- 通知内容：商户名称、新阶段、操作人
- 优先级：中

**D. 逾期任务警告**
- 触发时机：任务超过截止日期
- 通知内容：商户名称、逾期天数
- 优先级：紧急

#### 1.2.2 通知渠道

**纯前端版本（V2.0 P0）**:
1. **浏览器通知** - Web Notification API
2. **页面内提醒** - 扩展现有DeadlineAlert组件
3. **通知中心** - 新增通知列表页面

**后端版本（V2.0 P1+）**:
- WebSocket实时推送
- 邮件通知
- 短信通知（可选）

### 1.3 技术实现

#### 1.3.1 数据模型

```typescript
// 通知类型定义
interface Notification {
  id: string;
  type: 'task_deadline' | 'task_assigned' | 'task_status_change' | 'task_overdue';
  title: string;
  message: string;
  taskId: string;
  merchantName: string;
  priority: 'low' | 'medium' | 'high' | 'urgent';
  read: boolean;
  createdAt: string;
  scheduledFor?: string; // 定时通知
}

// 通知设置
interface NotificationSettings {
  enabled: boolean;
  browserNotifications: boolean;
  deadlineReminders: {
    enabled: boolean;
    days: number[]; // [3, 1, 0] 表示提前3天、1天、当天
  };
  taskAssignment: boolean;
  statusChanges: boolean;
  overdueAlerts: boolean;
}
```

#### 1.3.2 核心组件

**A. NotificationService (utils/notificationService.ts)**
```typescript
class NotificationService {
  // 请求浏览器通知权限
  async requestPermission(): Promise<boolean>

  // 发送浏览器通知
  sendBrowserNotification(notification: Notification): void

  // 检查任务截止日期，生成提醒
  checkDeadlines(): Notification[]

  // 保存通知到LocalStorage
  saveNotification(notification: Notification): void

  // 获取所有通知
  getNotifications(): Notification[]

  // 标记为已读
  markAsRead(notificationId: string): void
}
```

**B. NotificationCenter组件 (components/NotificationCenter.tsx)**
- 通知列表展示
- 未读数量badge
- 点击跳转到相关任务
- 全部标记为已读
- 清空通知

**C. NotificationBell组件 (components/NotificationBell.tsx)**
- 顶部导航栏通知图标
- 未读数量显示
- 下拉通知列表（最近5条）
- 点击查看全部通知

#### 1.3.3 定时检查机制

```typescript
// 使用setInterval定时检查
useEffect(() => {
  const checkInterval = setInterval(() => {
    const notifications = notificationService.checkDeadlines();
    notifications.forEach(n => {
      notificationService.sendBrowserNotification(n);
      notificationService.saveNotification(n);
    });
  }, 60000); // 每分钟检查一次

  return () => clearInterval(checkInterval);
}, []);
```

### 1.4 文件结构

```
mall-operation-system/
├── utils/
│   └── notificationService.ts          # 通知服务核心逻辑
├── components/
│   ├── NotificationCenter.tsx          # 通知中心页面
│   ├── NotificationBell.tsx            # 顶部通知图标
│   └── NotificationSettings.tsx        # 通知设置页面
├── app/
│   └── notifications/
│       └── page.tsx                    # 通知列表页面
└── types/
    └── index.ts                        # 添加Notification类型定义
```

---

## 功能B: 团队协作看板

### 2.1 功能概述

**目标用户**: 一线管理者

**核心价值**:
- 可视化团队工作负载，合理分配任务
- 实时追踪任务进度，及时发现瓶颈
- 支持任务转派，灵活调配资源

### 2.2 功能设计

#### 2.2.1 核心功能

**A. 团队成员管理**
- 添加/编辑团队成员
- 设置成员角色（运营经理/运营助理）
- 查看成员状态（在线/离线/忙碌）

**B. 任务看板视图**
- **按人员分组**: 每个成员的任务列表
- **按阶段分组**: Kanban风格（planning/executing/evaluating/completed）
- **按优先级分组**: urgent/high/medium/low
- **按风险等级分组**: high/medium/low

**C. 工作负载统计**
- 每个成员的任务数量
- 任务完成率
- 平均处理时间
- 工作量分布图表

**D. 任务操作**
- 任务转派（重新分配给其他成员）
- 任务认领（从待分配池中认领）
- 批量操作（批量分配、批量完成）

### 2.3 技术实现

#### 2.3.1 数据模型

```typescript
// 团队成员
interface TeamMember {
  id: string;
  name: string;
  role: 'assistant' | 'manager';
  avatar?: string;
  status: 'online' | 'offline' | 'busy';
  email?: string;
  phone?: string;
  createdAt: string;
}

// 团队
interface Team {
  id: string;
  name: string;
  members: TeamMember[];
  leaderId: string; // 团队负责人
  createdAt: string;
}

// 任务扩展（添加协作字段）
interface CollaborativeTask extends Task {
  watchers?: string[]; // 关注者ID列表
  transferHistory?: TaskTransfer[]; // 转派历史
}

// 任务转派记录
interface TaskTransfer {
  id: string;
  fromUserId: string;
  toUserId: string;
  reason: string;
  transferredAt: string;
}
```

#### 2.3.2 核心组件

**A. TeamBoard组件 (components/TeamBoard.tsx)**
- 看板主视图
- 支持拖拽排序（可选）
- 筛选和搜索
- 视图切换（按人员/按阶段/按优先级）

**B. MemberCard组件 (components/MemberCard.tsx)**
- 成员信息卡片
- 工作负载显示
- 任务列表
- 快速操作按钮

**C. TaskTransferModal组件 (components/TaskTransferModal.tsx)**
- 任务转派弹窗
- 选择目标成员
- 填写转派原因
- 确认转派

**D. TeamManagement组件 (components/TeamManagement.tsx)**
- 团队成员管理
- 添加/编辑/删除成员
- 角色设置

### 2.4 文件结构

```
mall-operation-system/
├── app/
│   └── team/
│       └── page.tsx                    # 团队协作看板页面
├── components/
│   ├── TeamBoard.tsx                   # 看板主组件
│   ├── MemberCard.tsx                  # 成员卡片
│   ├── TaskTransferModal.tsx           # 任务转派弹窗
│   └── TeamManagement.tsx              # 团队管理
├── utils/
│   └── teamService.ts                  # 团队服务逻辑
└── types/
    └── index.ts                        # 添加Team相关类型
```

---

## 技术架构

### 3.1 数据存储（纯前端版本）

**LocalStorage键值设计**:
```typescript
{
  "tasks": Task[],                      // 任务列表
  "notifications": Notification[],      // 通知列表
  "notificationSettings": NotificationSettings, // 通知设置
  "teamMembers": TeamMember[],          // 团队成员
  "teams": Team[],                      // 团队列表
  "currentUser": string                 // 当前用户ID（模拟）
}
```

### 3.2 状态管理

使用React Hooks:
- `useState` - 组件本地状态
- `useEffect` - 副作用处理（定时检查、数据加载）
- `useMemo` - 计算缓存（统计数据）
- `useCallback` - 函数缓存

### 3.3 性能优化

1. **防抖和节流**: 搜索、筛选等操作
2. **虚拟滚动**: 长列表渲染（如果需要）
3. **懒加载**: 图片、组件按需加载
4. **缓存**: useMemo缓存计算结果

---

## 实施顺序

### Phase 1: 任务提醒和推送 (2-3天)

**Day 1** ✅:
- [x] 创建Notification类型定义（已重命名为AppNotification避免与浏览器API冲突）
- [x] 实现NotificationService核心逻辑
- [x] 实现浏览器通知权限请求
- [x] 实现任务截止日期检查
- [x] 创建NotificationBell组件
- [x] 集成到MainLayout（桌面端在侧边栏，移动端在右上角）
- [x] 创建通知中心页面（/notifications）

**Day 2** (待完成):
- [ ] 在MainLayout中添加定时检查机制
- [ ] 实现浏览器通知发送功能
- [ ] 创建NotificationSettings组件
- [ ] 实现通知设置保存和加载

**Day 3** (待完成):
- [ ] 测试各种通知场景（deadline, assigned, status change, overdue）
- [ ] 完善移动端通知体验
- [ ] 添加通知音效（可选）
- [ ] 完成Phase 1文档

### Phase 2: 团队协作看板 (3-4天)

**Day 1**:
- [ ] 创建Team相关类型定义
- [ ] 实现TeamService核心逻辑
- [ ] 创建TeamManagement组件
- [ ] 实现成员添加/编辑/删除

**Day 2**:
- [ ] 创建TeamBoard主组件
- [ ] 实现按人员分组视图
- [ ] 实现MemberCard组件
- [ ] 显示成员任务列表

**Day 3**:
- [ ] 实现TaskTransferModal组件
- [ ] 实现任务转派逻辑
- [ ] 实现工作负载统计
- [ ] 添加图表展示

**Day 4**:
- [ ] 实现其他视图（按阶段/按优先级）
- [ ] 移动端适配
- [ ] 性能优化
- [ ] 全面测试

---

## 验证方案

### 5.1 功能测试

**任务提醒和推送**:
1. 创建一个即将到期的任务（1天后），验证是否收到提醒
2. 创建一个已逾期的任务，验证是否显示警告
3. 修改通知设置，验证是否生效
4. 在通知中心查看所有通知
5. 点击通知跳转到对应任务

**团队协作看板**:
1. 添加3个团队成员（2个助理，1个经理）
2. 创建5个任务，分配给不同成员
3. 在看板中查看每个成员的任务
4. 转派一个任务给其他成员
5. 查看工作负载统计图表
6. 切换不同的视图模式

### 5.2 性能测试

1. 创建100个任务，测试看板加载速度
2. 创建50条通知，测试通知列表渲染性能
3. 测试定时检查机制的CPU占用

### 5.3 兼容性测试

**浏览器**:
- Chrome (最新版)
- Safari (最新版)
- Firefox (最新版)
- Edge (最新版)

**设备**:
- 桌面端 (1920x1080, 1440x900)
- 平板端 (iPad, 768x1024)
- 移动端 (iPhone, 375x667)

---

## 注意事项

### 6.1 浏览器通知权限

- 用户首次使用时需要授权
- 如果用户拒绝，需要提供引导说明
- 某些浏览器（如Safari）对通知有限制

### 6.2 LocalStorage限制

- 容量限制：5-10MB
- 数据仅存储在本地，无法跨设备同步
- 清除浏览器数据会丢失所有信息

### 6.3 多用户模拟

- 纯前端版本使用currentUser字段模拟当前用户
- 可以通过切换currentUser来模拟不同用户登录
- 后续接入后端时需要替换为真实的用户认证

---

**最后更新**: 2026-01-26
**负责人**: Claude Sonnet 4.5
**预计完成时间**: 5-7天
