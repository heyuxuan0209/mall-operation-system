# 商户智运Agent 改版 Brief

你现在要基于现有的 `商户智运Agent` 产品，完成一版面向**对外销售展示**的产品改版方案与界面改版建议。

## 1. 项目背景

`商户智运Agent` 是一个服务商场运营方的产品，当前已有一定业务基础，包括：

- 运营总览
- 健康度监控
- 风险与派单
- 帮扶任务中心
- 帮扶档案库
- 现场巡店
- AI帮扶知识库

当前主要客户是 **商场运营方**，后期希望扩展到 **品牌方**。

这次改版的主要目标不是做一个更完整的后台系统，而是把产品升级成一个更适合**销售演示、客户打动、价值表达更强**的 AI 产品。

## 2. 改版目标

请把 `商户智运Agent` 从现在的：

`商户运营后台 + AI助手`

升级成：

`面向商场运营方的商户经营决策中枢`

一句话定位建议：

`让商场从看见问题，升级为自动发现问题、判断问题、生成方案、推动执行、沉淀方法的 AI 经营班子。`

## 3. 需要重点参考的对标方向

请参考 `HIpilot` 这类产品的设计逻辑，但不要简单照搬视觉。重点学习它的产品组织方式和交互逻辑：

### HIpilot 值得借鉴的点

1. 它是 **场景优先**，不是功能优先  
   不是先展示通用能力，而是直接给出 `库存优化 / 拓店选址 / 供应链协同 / 食材管控 / 退货预警` 等业务场景。

2. 它把 **多 AI 专家团队协作** 做成了可视化  
   用户能看到有哪些 Agent 在参与、各自职责是什么、谁已经完成、谁在分析。

3. 它不是只给分析，而是给 **可拍板的方案**  
   会输出方案 A/B、预期收益、周期、风险、推荐意见。

4. 它把 **执行动作** 放进主流程  
   不是只给建议，而是能直接进入 `马上执行 / 启动任务 / 生成动作`。

5. 它强化了 **组织记忆/案例沉淀**  
   不只是一次分析，而是把老板经验、成功案例沉淀为系统可复用的记忆。

6. 它卖的是 **老板视角的经营能力**  
   像一支 AI 经营班子，而不是一个聊天助手。

## 4. 当前产品的主要问题

虽然 `商户智运Agent` 已经有业务基础，但目前更像：

`经营看板 + AI助手 + 任务系统`

对外销售时不够打动人，主要问题包括：

1. AI 还是“外挂助手”，不是主工作流
2. 产品主视角还是后台看板，不是“决策中枢”
3. 缺少多 Agent 协同感
4. 缺少“方案比较 + 推荐方案 + 一键执行”的决策收口
5. 案例库/档案库更像静态资料，而不是动态组织记忆
6. 缺少让管理者一眼感知价值的“老板模式”

## 5. 改版后的核心定位

请围绕下面这句去重构产品：

`商户智运Agent 不是帮商场看数据的系统，而是帮商场做经营决策、推执行落地、沉淀组织经验的 AI 经营中枢。`

## 6. 目标用户

### 当前主目标用户

- 商场总经理
- 商场运营负责人
- 招商/运营管理者

他们最关心的是：

- 哪些商户有风险
- 哪些商户值得优先帮扶
- 为什么出问题
- 该怎么干预
- 谁去执行
- 经验能不能复制

### 未来扩展用户

- 连锁品牌总部
- 区域运营负责人
- 品牌门店经营管理者

所以本次设计请**以商场运营方为主视角**，但底层结构要尽量为品牌方扩展留接口。

## 7. 产品战略建议

请把整体产品重构成四层闭环：

1. `AI 感知层`
   自动汇总商户经营、客流、销售、投诉、巡店、活动、租约等数据，形成经营画像

2. `AI 决策层`
   多 Agent 协同完成风险识别、根因诊断、方案生成、优先级排序

3. `AI 执行层`
   把建议转成帮扶任务、派单、巡店、沟通动作、复盘动作

4. `AI 记忆层`
   把成功/失败案例沉淀为组织记忆，支持下次自动调用

## 8. 改版后的一级信息架构建议

请优先基于下面的信息架构来设计：

1. `经营驾驶舱`
2. `AI 决策工作台`
3. `执行推进中心`
4. `组织记忆中心`
5. `经营设置`

原有模块可做如下映射：

- `运营总览` -> 并入 `经营驾驶舱`
- `健康度监控` -> 作为 `经营驾驶舱` 下钻能力
- `风险与派单` -> 并入 `执行推进中心`
- `帮扶任务中心` -> 并入 `执行推进中心`
- `帮扶档案库` -> 升级为 `组织记忆中心`
- `现场巡店` -> 作为 `执行推进中心` 的执行场景
- `AI帮扶知识库` -> 并入 `组织记忆中心`

## 9. 四个核心页面设计要求

### A. 经营驾驶舱

这是首页。

目标：

- 给管理层一个“今天最该处理什么”的入口
- 不只是看图表，而是进入经营决策

建议结构：

- 今日经营晨报
- 今日重点问题卡片
- 商户健康全景
- AI 推荐优先级
- 当前执行进展

希望体现的感觉：

`不是数据总览，而是经营待办 + 决策入口`

### B. AI 决策工作台

这是改版的核心页面。

目标：

- 展示单个商户/问题的多 Agent 协同分析过程
- 把 AI 分析做成“经营班子会商现场”

请重点设计以下内容：

- 问题触发
- 根因诊断
- 多专家协作过程
- 方案 A/B 对比
- 推荐方案
- 预期收益/周期/风险
- 一键发起执行
- 右侧展示参与 Agent、职责、状态、协作时间线、参考案例

请明确把“AI 多专家团队”做成界面主角。

### C. 执行推进中心

目标：

- 证明产品不只会分析，还能推动落地

请体现：

- 任务来源于哪个问题
- 推荐方案如何转成任务
- 任务负责人、进度、截止时间
- 巡店动作、帮扶动作、沟通动作、复盘动作
- 可按商户 / 问题类型 / 负责人 / 状态查看

### D. 组织记忆中心

目标：

- 把“帮扶档案库/知识库”升级成 AI 可调用的组织记忆系统

请体现：

- 成功案例库
- 失败案例复盘
- 策略模板库
- 适用条件
- 相似案例推荐
- 被 AI 调用的痕迹

每个案例最好包括：

- 案例ID
- 商户画像
- 问题定义
- 根因诊断
- 采用策略
- 执行动作
- 结果指标
- 适用条件

## 10. 多 Agent 角色建议

请为产品设计一组固定的“经营专家 Agent”，用于支撑销售展示中的多智能体协作感。

建议角色可包括：

- 风险诊断师
- 商户经营顾问
- 活动策略师
- 巡店督导
- 任务调度官
- 案例记忆官

如果你认为需要，也可以适度微调命名，但要保持：

- 易理解
- 有经营感
- 适合销售展示

## 11. 推荐优先展示的业务场景

本次改版请优先围绕商场运营方最容易买单的几个场景组织界面：

1. `高风险商户预警与帮扶`
2. `续约风险识别与干预`
3. `活动后经营复盘与优化`
4. `巡店问题闭环与经验沉淀`

可选扩展场景：

- 招商补位建议
- 商户分层运营
- 客诉异常干预

## 12. 视觉与交互气质要求

请避免把产品继续做成“传统后台管理系统 + 一个 AI 聊天框”。

期望气质：

- 更像经营决策中枢
- 更像一支 AI 经营班子在工作
- 更适合向客户演示
- 更能体现高价值和智能化

交互上请重点强化：

- 场景化入口
- 决策流程感
- 专家协作感
- 推荐方案高亮
- 一键执行
- 案例调用痕迹

## 13. 输出要求

请基于以上背景，直接输出一版完整的改版方案，尽量包括：

1. 新的产品定位与一句话描述
2. 改版后的信息架构
3. 首页/经营驾驶舱方案
4. AI 决策工作台方案
5. 执行推进中心方案
6. 组织记忆中心方案
7. 多 Agent 设计方案
8. 适合销售展示的业务场景组织方式
9. 未来面向品牌方扩展的预留建议
10. 如果可以，补充页面框架或模块草图思路

## 14. 额外要求

- 请优先从“销售展示价值”出发，而不只是从系统功能完整性出发
- 请避免泛泛而谈，要尽量具体到页面组织和交互逻辑
- 请尽量帮助把产品从“运营工具”升级成“AI 决策产品”
- 如果要给文案建议，请使用更像 toB 高价值产品的表达方式

